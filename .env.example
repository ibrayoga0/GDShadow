VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Comma-separated list of admin emails allowed to access dashboard. Leave empty to allow any authenticated user.
VITE_ADMIN_EMAILS=admin@example.com

# Only needed on serverless (for click logging in /api/d/[fileId].js)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Shadow Player optional external proxy base (if hosting stream endpoint elsewhere)
# If empty, will use local /api/stream/[fileId]
VITE_PROXY_BASE_URL=
